# Site settings
title: iOS Internship Roadmap

# Build settings
markdown: kramdown
highlighter: rouge

# (Remove or comment this out if you don't want Minima)
# theme: minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Syntax highlighting
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Collections
collections:
  tutorials:
    output: true
